[pytest]
addopts = --tb=short -q --cov=src --cov-report=term-missing:skip-covered --cov-fail-under=90
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
  integration: mark a test as an integration test.
  unit: mark a test as a unit test.
