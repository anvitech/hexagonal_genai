"""
Application services packages.

This package contains all application services that will be used by
user interfaces.

- ChatService: A service that handles the user message and generates a response using
a LLM model provider.
- ConversationService: A service that manages the conversations between the user
and the chatbot.
"""